# 安全政策

## 受支持的版本

| 版本 | 安全更新 |
|------|----------|
| 1.0.x | ✅ |
| 0.2.x | ❌（建议升级到 1.0.0） |

## 报告漏洞

发现安全问题时：

1. **不要**在公开 issue 里披露细节；
2. 优先用 GitHub 的 [Security Advisory](https://github.com/x1303145921/fconv/security/advisories/new) 私下报告；
3. 如果不是敏感问题，也可以直接开 issue（请去掉敏感信息）。

请在报告里尽量写明：复现步骤、影响范围、涉及版本、以及你建议的修复方向。
我们会在确认后尽快给出处理计划，并在修复版本里致谢（如果你愿意）。

## 安全设计

fconv 是一个**本地工具**，安全边界按「本机可信、网络不可信」设计：

- **只监听回环地址**：服务默认绑定 `127.0.0.1:8765`，不对外网开放；
- **不上传、不联网**：程序运行期不会把文件或使用数据发到任何远端（依赖安装除外）；
- **文件名净化**：上传文件名经过清洗，`../../x.png` 这类路径穿越会被压成 `x.png`，
  且中文、空格等合法字符会保留；
- **大小限制**：单请求上限 200MB，超出返回明确的中文提示而不是 500；
- **错误不泄漏内部结构**：用户看到的是中文提示，堆栈只写进本机 `logs/fconv.log`；
- **转换器不抛异常**：所有失败都收敛为 `ConvertResult`，避免服务因单个坏文件崩溃；
- **产物按时清理**：`src/uploads/` 下按任务分目录保存，默认 24 小时后自动清理；
- **不执行用户输入**：所有外部程序调用（FFmpeg）使用参数数组形式，不经 shell 拼接。

### 需要注意的边界

- 如果你把服务改成监听 `0.0.0.0`，局域网内任何人都能上传文件并触发转换——
  这超出了本项目的设计假设，请自行加访问控制（反向代理 + 鉴权）。
- 转换过程会消耗 CPU/内存，恶意的大文件或超多并发可能造成资源占用；
  对不可信来源的输入，建议限制并发数（`/api/batch-convert` 内部上限为 8 线程）。
- 依赖自身的漏洞请参考各组件公告（Pillow、pypdf、Flask、FFmpeg）。

## 依赖与许可

第三方组件及其许可见 [THIRD-PARTY-NOTICES.txt](THIRD-PARTY-NOTICES.txt)。
其中 PyMuPDF 为 AGPL-3.0，本项目不分发、不自动安装。

## 已知安全问题

截至 2026-09-20（v1.0.0）：未发现已知安全问题。

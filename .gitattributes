# 统一文本属性，避免不同系统 clone 后出现整文件 diff
* text=auto eol=lf

# Windows 启动器（bat/cmd/vbs）为 GBK + CRLF，必须按二进制原样保存，
# 否则任何换行归一化都会让它们在中文字符下出现乱码。
*.bat binary
*.cmd binary
*.vbs binary

# 二进制资源
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.zip binary
*.pdf binary

# 图标矢量源按文本处理，方便 review
*.svg text eol=lf

# GitHub 语言统计：文档不计入代码占比
*.md linguist-documentation
*.txt linguist-documentation

# 面向 Windows 记事本的 README.txt：与启动器同一口径，必须 GBK + CRLF。
# 上面的 `* text=auto eol=lf` 会把它归一化成 LF，导致 Linux 克隆后
# check_release.py 的「启动器编码」检查失败（CI 实测踩到），这里显式盖掉。
README.txt text eol=crlf
